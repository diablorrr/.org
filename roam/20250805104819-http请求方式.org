:PROPERTIES:
:ID:       332d23bd-0c98-4686-9b0b-c43a1e85e324
:END:
#+title: HTTP请求方式
#+filetags: network

* HTTP请求方式
*GET*    ：
1. 语义 是 获取数据
2. 数据是键值对形式且暴露在URL中 -> 安全性不好
3. 幂等
   - 幂等 :: 多次请求，结果相同

*POST*   ：
1. 语义 是 提交数据
2. 数据可以是结构化的JSON数据且在请求体中 -> 安全性好
3. 非幂等

*PUT*    ：修改数据
*DELETE* ：删除数据
